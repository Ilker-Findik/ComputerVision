# MNIST

MNIST preprocessing and dense-network experiment.

## Run

- Execute `ann.py` from this folder or from the repository root.
- The script downloads MNIST through Keras on first run.

## Notes

- The pipeline applies histogram equalization, blur, and edge extraction before flattening the images.
- Keep generated outputs and checkpoints out of the repository.
